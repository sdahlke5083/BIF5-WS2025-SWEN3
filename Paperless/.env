# Docker registry prefix (optional, e.g. "myregistry.com/", for images not hosted on dockerhub)
DOCKER_REGISTRY=

# PostgreSQL configuration
POSTGRES_DB=paperless
POSTGRES_USER=paperless
POSTGRES_PASSWORD=paperless

# Adminer configuration
ADMINER_DEFAULT_SERVER=paperless-db
ADMINER_DEFAULT_DRIVER=pgsql
ADMINER_DESIGN=dracula

# RabbitMQ configuration (defaults used by services and Dockerfiles)
# Host that services should use to reach RabbitMQ (matches container_name)
RABBITMQ_HOST=paperless-rabbitmq
RABBITMQ_PORT=5672
RABBITMQ_USER=paperless
RABBITMQ_PASS=paperless
# Exchange used for task routing
RABBITMQ_EXCHANGE=tasks
RABBITMQ_EXCHANGE_TYPE=direct
# Per-service consumer queue names
RABBITMQ_OCR_QUEUE=ocr-queue
RABBITMQ_GENAI_QUEUE=genai-queue
RABBITMQ_REST_QUEUE=rest-queue

# REST API URL for internal workers to call
REST_API_URL=http://paperless-rest:8080

# Postgres host/port for services
POSTGRES_HOST=paperless-db
POSTGRES_PORT=5432

# MinIO settings used by services
MINIO_ENDPOINT=paperless-minio:9000
MINIO_BUCKET=paperless

# JWT defaults for REST API (override in production)
Jwt__Key=please_change_this_secret_in_production
Jwt__Issuer=paperless.local
Jwt__Audience=paperless.local

# MinIO configuration
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin

# GenAI / Google Gemini configuration
GENAI_API_KEY=Test
GENAI_MODEL=gemini-2.5-flash-lite

# Elasticsearch configuration
# Hostname used by the docker network (service name in docker-compose)
ELASTICSEARCH_HOST=paperless-elasticsearch
ELASTICSEARCH_PORT=9200
